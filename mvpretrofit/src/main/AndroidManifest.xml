<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.wgl.mvpretrofit">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <!--
        清单配置文件配置参数（复制到项目的manifest文件中）
        UI切图，按照1080*1920给的，可以是其他的
        <meta-data
            android:name="design_width"
            android:value="1080" />
        <meta-data
            android:name="design_height"
            android:value="1920" />
            -->
    </application>

</manifest>
